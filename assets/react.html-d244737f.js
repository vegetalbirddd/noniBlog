import{_ as e,o as a,c,b as t}from"./app-2eb9f1ef.js";const i={},o=t('<h2 id="react" tabindex="-1"><a class="header-anchor" href="#react" aria-hidden="true">#</a> react</h2><h3 id="重要主题" tabindex="-1"><a class="header-anchor" href="#重要主题" aria-hidden="true">#</a> 重要主题</h3><ul><li>基础知识：JSX、组件、渲染（虚拟 DOM、协调）</li><li>组件设计：声明式思考、props、state、结构</li><li>React Hooks：常见 Hooks（例如 <code>useState</code>、<code>useEffect</code>），Hooks 规则，最佳实践</li><li>受控组件和非受控组件的区别</li><li>React 中的事件处理</li><li>常见陷阱（例如，陈旧的闭包、不必要的重新渲染）</li><li>性能优化：记忆化、懒加载、React Profiler</li></ul><h3 id="react-vs-vue" tabindex="-1"><a class="header-anchor" href="#react-vs-vue" aria-hidden="true">#</a> React VS Vue</h3><ul><li>react 和 Vue 都是组件化框架，但状态模型不同。React 是函数式 UI，状态分散在各组件中，通过 setState 触发组件重新执行来更新 UI；而 Vue 基于响应式系统，通过依赖收集自动追踪数据与视图的关系，数据变化时自动触发更新。React 更偏显式数据流，Vue 更偏自动响应式绑定。</li><li>React 模板语法是 JSX，all in js，Vue 模板语法是 Template、js、css，支持指令</li><li>React 生命周期：初始化、更新、卸载，Vue 生命周期：创建、挂载、更新、销毁</li></ul><h3 id="jsx" tabindex="-1"><a class="header-anchor" href="#jsx" aria-hidden="true">#</a> JSX</h3><p>JSX 是 JavaScript 的语法扩展，用于描述 UI 结构。它会先经过 Babel 编译成 React.createElement 调用。执行后会生成 ReactElement 对象，也就是虚拟DOM（Virtual DOM）。React 再基于这些 虚拟DOM 进行 diff，最终通过 ReactDOM 将变化同步到真实 DOM。</p><p>1、为什么不直接转换成DOM</p><p>先转化成JS，方便后续有变化的时候进行计算，如果直接转换成DOM，在有变化的时候就要直接操作DOM，这样就会导致需要重排很多次，性能比较不好</p><p>再者就是，不同的渲染器会将虚拟DOM转成对应平台的元素，不一定是浏览器的DOM。</p><p>2、react的render为什么只能返回单一的一个JSX元素，不能并列？</p><p>JSX经编译后本质上是一个函数调用，JS的return只能返回一个值，虚拟DOM是一个树状结构，只能有一个根节点。</p><p>React 的虚拟 DOM 是一棵 <strong>树状结构</strong>，树必须有且仅有一个根节点。如果允许并列多个元素，虚拟 DOM 会变成 “森林”（多棵树），这会导致：</p><ul><li><p>Diff 算法无法高效对比（树的遍历依赖单一根节点）</p></li><li><p>状态管理、生命周期等逻辑难以统一附着在 “多个根” 上</p></li><li><p>与 React 的协调（Reconciliation）机制冲突（需要明确的根节点作为更新起点）</p></li></ul><h3 id="无状态组件和有状态组件" tabindex="-1"><a class="header-anchor" href="#无状态组件和有状态组件" aria-hidden="true">#</a> 无状态组件和有状态组件</h3><p>无状态组件（Stateless Component）是最基础的组件形式，由于没有状态的影响所以就是纯静态展示的作用</p><p>如果组件内部包含状态且状态随着事件或者外部的消息而发生改变的时候，这就构成了有状态组件</p><h3 id="受控组件和非控组件" tabindex="-1"><a class="header-anchor" href="#受控组件和非控组件" aria-hidden="true">#</a> 受控组件和非控组件</h3><p>在使用表单来收集用户输入时，例如<code>&lt;input&gt;&lt;select&gt;&lt;textearea&gt;</code>等元素都要绑定一个 <code>change</code> 事件，当表单的状态发生变化，就会触发 <code>onChange</code> 事件，更新组件的 <code>state</code>。这种组件在 React 中被称为<strong>受控组件</strong>。</p><p>如果一个表单组件没有 <code>value props</code>（单选和复选按钮对应的是<code>checked props</code>）时，就可以称为<strong>非受控组件</strong>。在非受控组件中，可以使用一个 <code>ref</code>来从 <code>DOM</code> 获得表单值。而不是为每个状态更新编写一个事件处理程序。</p><h3 id="react-组件的通信方式有哪些" tabindex="-1"><a class="header-anchor" href="#react-组件的通信方式有哪些" aria-hidden="true">#</a> React 组件的通信方式有哪些？</h3><ul><li>父子通信：<code>props/props callback</code>，<code>context</code>，<code>ref</code>，<code>event bus</code>，<code>状态管理库</code>。</li><li>兄弟通信：<code>event bus</code>，<code>公共父组件 context</code>，<code>状态管理库</code></li><li>隔代（跨级）通信：<code>event bus</code>，<code>context</code>,<code>状态管理库</code></li></ul><h3 id="函数组件和类组件本质的区别是什么" tabindex="-1"><a class="header-anchor" href="#函数组件和类组件本质的区别是什么" aria-hidden="true">#</a> 函数组件和类组件本质的区别是什么？</h3><p>对于类组件来说，底层只需要实例化一次，实例中保存了组件的 state 等状态。对于每一次更新只需要调用 render 方法以及对应的生命周期就可以了。</p><p>但是在函数组件中，每一次更新都是一次新的函数执行，一次函数组件的更新，里面的变量会重新声明。为了能让函数组件可以保存一些状态，执行一些副作用钩子，React Hooks 应运而生，它可以帮助记录 React 中组件的状态，处理一些额外的副作用。</p><h3 id="常用的-react-hooks" tabindex="-1"><a class="header-anchor" href="#常用的-react-hooks" aria-hidden="true">#</a> 常用的 React Hooks</h3><ul><li><code>状态钩子 (useState)</code>: 用于定义组件的 State，类似类定义中 this.state 的功能</li><li><code>useReducer</code>：用于管理复杂状态逻辑的替代方案，类似于 Redux 的 reducer。</li><li><code>生命周期钩子 (useEffect)</code>: 类定义中有许多生命周期函数，而在 React Hooks 中也提供了一个相应的函数 (useEffect)，这里可以看做componentDidMount、componentDidUpdate和componentWillUnmount的结合。</li><li><code>useLayoutEffect</code>：与 useEffect 类似，但在浏览器完成绘制之前同步执行。</li><li><code>useContext</code>: 获取 context 对象，用于在组件树中获取和使用共享的上下文。</li><li><code>useCallback</code>: 缓存回调函数，避免传入的回调每次都是新的函数实例而导致依赖组件重新渲染，具有性能优化的效果；</li><li><code>useMemo</code>: 用于缓存计算结果，避免重复计算昂贵的操作。</li><li><code>useRef</code>: 获取组件的真实节点；用于在函数组件之间保存可变的值，并且不会引发重新渲染。</li><li><code>useImperativeHandle</code>：用于自定义暴露给父组件的实例值或方法。</li><li><code>useDebugValue</code>：用于在开发者工具中显示自定义的钩子相关标签。</li></ul><h3 id="fiber" tabindex="-1"><a class="header-anchor" href="#fiber" aria-hidden="true">#</a> Fiber</h3><p>Fiber是一种协调器架构，把同步不可中断的渲染任务拆分成多个小任务，通过时间切片和任务调度机制来避免长时间阻塞线程。</p><h3 id="为什么设计-fiber-react-为什么需要-fiber" tabindex="-1"><a class="header-anchor" href="#为什么设计-fiber-react-为什么需要-fiber" aria-hidden="true">#</a> 为什么设计 Fiber，React 为什么需要 Fiber ？</h3><p>解题思路</p><ol><li>Fiber 设计之前的痛点</li><li>Fiber 怎么解决的这些痛点</li></ol><p>React v15 及之前版本使用的协调算法（diff 算法） 是 Stack Reconciler ，有以下的 3 个痛点：</p><ul><li>递归对比虚拟 DOM 树，执行栈会越来越深。</li><li>同步更新DOM，不能中断，中断后也不能恢复。</li><li>JS 代码执行时间长，会持续占用主线程，出现渲染卡顿。</li></ul><p>当遇到进程同步阻塞的问题时，<strong>任务分割、异步调用</strong> 和 <strong>缓存策略</strong> 是三个显著的解决思路。因此，为了解决以上的痛点问题，React 设计了 Fiber 架构， 把更新、渲染过程拆分为一个个小块的任务，通过合理的调度机制来调控时间，指定任务执行的时机，实现异步可中断执行，从而降低页面卡顿的概率，提升页面交互体验。通过 Fiber 架构，让 reconcilation 过程变得可被中断，适时地让出 CPU 执行权，可以让浏览器及时地响应用户的交互。（就是使 JS 的执行变成可控，不希望 JS 不受控制地长时间执行）。</p><p>Fiber 的主要目标是实现虚拟 DOM 的增量渲染，能够将渲染工作拆分成块并将其分散到多个帧的能力。在新的更新到来时，能够暂停、中止和复用工作，能为不同类型的更新分配优先级顺序的能力，因此 这种 Fiber reconciler 很好的解决了Stack Reconciler 的问题。</p><h3 id="fiber-为什么设计成链表结构-为什么中断执行后可以恢复" tabindex="-1"><a class="header-anchor" href="#fiber-为什么设计成链表结构-为什么中断执行后可以恢复" aria-hidden="true">#</a> Fiber 为什么设计成链表结构？为什么中断执行后可以恢复？</h3><p>能够将执行到一半的工作保存在内存的链表中。</p><p>当 React 停止并完成保存的工作后，让出时间片去处理一些其他优先级更高的事情。之后，在重新获取到可用的时间片后，它能够根据之前保存在内存的上下文信息通过快速遍历的方式找到停止的 fiber 节点并继续工作。由于在此阶段执行的工作并不会导致任何用户可见的更改，因为并没有被提交到真实的 DOM。所以，这种链表结构的 fiber 能让调度实现暂停、中止以及重新开始等增量渲染的能力。</p>',39),r=[o];function d(l,n){return a(),c("div",null,r)}const h=e(i,[["render",d],["__file","react.html.vue"]]);export{h as default};
